<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/background"
    tools:context="pl.proaktyw.proaktyw.AddChallenge"
    android:id="@+id/add_challenge">

    <android.support.v7.widget.Toolbar
        android:id="@+id/add_challenge_editor_toolbar"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/background"
        app:navigationIcon="@drawable/arrow_back">

    </android.support.v7.widget.Toolbar>


    <ListView
        android:id="@+id/add_challenge_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/add_challenge_editor_toolbar"
        android:layout_above="@+id/add_challenge_editor_bottom_layout"
        android:layout_marginStart="8sp"
        android:layout_marginEnd="8sp"
        android:layout_marginTop="8sp"
        android:layout_marginBottom="8sp"
        android:choiceMode="singleChoice"
        android:listSelector="@color/background_second"/>

    <RelativeLayout
        android:id="@+id/add_challenge_editor_bottom_layout"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        android:background="@color/background"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/add_challenge_info_button"
            android:background="@color/background"
            android:src="@drawable/info_ic"
            android:layout_width="?attr/actionBarSize"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentStart="true"
            android:layout_centerInParent="true"
            android:layout_marginStart="8sp"
            android:text="info" />

        <ImageButton
            android:id="@+id/add_challenge_button"
            android:background="@color/background"
            android:src="@drawable/confirm_ic"
            android:layout_width="?attr/actionBarSize"
            android:layout_height="?attr/actionBarSize"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="8sp"
            android:text="Add" />

    </RelativeLayout>


</RelativeLayout>
